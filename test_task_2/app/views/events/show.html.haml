%h2 Event: "#{@event.title}"

:css
  table{text-align: center;}
%table
  %tr
    %th date
    %th type_repeat
  %tr
    %td= @event.date
    %td= @event.type_repeat

%p
  = link_to "Редактировать", edit_event_path(@event), class: "btn btn-primary"