%h2 Account settings

= gravatar_for(@user)

= simple_form_for @user do |f|
  -# = f.input :email
  = f.input :name
  = f.input :last_name
  = f.input :password
  = f.input :password_confirmation
  = f.submit "Сохранить", class: "btn btn-primary"