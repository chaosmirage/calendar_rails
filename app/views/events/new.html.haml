%h2 Create new event

= simple_form_for @event do |f|
  = f.input :title
  = f.input :date, as: :date_picker
  = f.input :type_repeat, collection: @type_repeat, selected: @type_repeat[:daily]

  = f.submit "Создать событие", class: "btn btn-primary"
