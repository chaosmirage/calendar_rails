%h2 Account settings

.text-center
  = gravatar_for(@user)

= simple_form_for @user do |f|
  = render partial: "partials/users/form", locals: {f: f}
  = f.submit "Сохранить", class: "btn btn-primary"
